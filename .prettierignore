# Build output
dist
build

# Dependencies
node_modules

# Generated / lockfiles
package-lock.json
pnpm-lock.yaml
yarn.lock

# Static assets
public

# Letters are prose — keep them exactly as they were typed (or committed by the
# issue-sync workflow), never reflowed.
src/letters
